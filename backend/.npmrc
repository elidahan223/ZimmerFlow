# Skip Chromium download in production installs (EB runs npm install --production
# so puppeteer lives in devDependencies and won't be installed there, but this
# protects against accidental re-introduction or a non-production install on EB).
puppeteer_skip_download=true
