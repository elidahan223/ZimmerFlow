# Increase EB command timeout so npm install of @sparticuz/chromium
# (~50MB Chromium binary) on a small t3.micro instance doesn't time out.
# Default is 600s (10 min); 1800s = 30 min gives plenty of headroom.
option_settings:
  aws:elasticbeanstalk:command:
    Timeout: 1800
